generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// DP00 foundation only. Business models (Customer, Booking, Payment, etc.)
// are added by later Developer Packages. Do not invent domain tables here.
