import { AppShell } from '@sp2036/ui';

export default function HomePage() {
  return (
    <AppShell productName="SP2036 MVP" title="Admin foundation shell">
      <p>
        Administration portal shell. Dashboard, authentication, and operational modules
        are not implemented in DP00.
      </p>
    </AppShell>
  );
}
